server:
  host: 0.0.0.0
  port: 8080
  mode: debug  # debug or release
  jwt_secret: your-secret-key-change-in-production
  allow_origins:
    - "*"

database:
  driver: mysql  # mysql or postgres
  host: localhost
  port: 3306
  database: xgent
  username: root
  password: "2475Dy1098"

redis:
  host: localhost
  port: 6379
  password: ""
  db: 0

orchestrator:
  workers: 10
  queue_size: 100
  workspace_dir: /tmp/xgent-workspaces

agno:
  openai_key: ${OPENAI_API_KEY}
  anthropic_key: ${ANTHROPIC_API_KEY}

docker:
  enabled: false
  image: xgent-executor:latest
  cpu_limit: 2
  memory_limit: 4096  # MB

git:
  clone_timeout: 300  # seconds
  default_branch: main

logging:
  level: info
  format: json
  output: stdout
